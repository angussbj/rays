export const xyOffset = 0;
export const uvOffset = 4 * 2;
export const squareStride = 4 * 4;

// prettier-ignore
export const square = new Float32Array([
  // x     y     u   v
    -0.8, -0.8,
     0.8, -0.8,
     0.8,  0.8,
    -0.8, -0.8,
     0.8,  0.8,
    -0.8,  0.8,
  ]);
